# Optional deployment theme

The public build does not ship branded deployment artwork. Self-hosters may
provide `background.png` and `manual-drop.png` in this directory and build with
`DMXTRACT_CUSTOM_THEME=true`. Keep asset ownership and licensing records in the
deploying repository.
