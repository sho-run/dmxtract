{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The informational bridge window has no shell, autostart, or deep-link permissions — those plugins are only ever driven from Rust (`app.autolaunch()`, `app.deep_link()`), never invoked from this window's static, script-free HTML.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
